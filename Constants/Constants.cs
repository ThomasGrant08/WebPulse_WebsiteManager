using Microsoft.AspNetCore.Mvc;

namespace WebPulse_WebManager.Constants
{
    public enum Roles
    {
        SystemOwner,
        GlobalAdmin,
        Admin,
        Moderator,
        User
    }

    public class Constants
    {
    }
}
